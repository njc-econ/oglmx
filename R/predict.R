

predict.oglmx <- function(object, newdata = NULL, type = c("link", "response"),
                                  outcome = "all", na.action, ...){
  objectTerms <- terms(object)
  if (missing(newdata) || is.null(newdata)){
    
  } else {
    
  }
}